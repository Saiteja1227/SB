spring.application.name=firstproject

# PostgreSQL Database Configuration
# Uses environment variables in production, falls back to defaults for local development
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://dpg-d6g15adm5p6s7393jh0g-a.oregon-postgres.render.com:5432/myapp_1vxx}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:myapp_1vxx_user}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:BTUEi0O5iR7litF5365MvJV9USj0V0LC}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate Configuration
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=${SPRING_JPA_HIBERNATE_DDL_AUTO:update}
spring.jpa.show-sql=${SPRING_JPA_SHOW_SQL:true}
spring.jpa.properties.hibernate.format_sql=true

# Server Configuration
server.port=${PORT:8080}

# JWT Configuration
jwt.secret=${JWT_SECRET:SmartCart2026SecureSecretKeyForJWTTokenGenerationMustBe256BitsOrMore}
jwt.expiration=${JWT_EXPIRATION:86400000}

# Firebase Configuration
# Set FIREBASE_SERVICE_ACCOUNT environment variable with your service account JSON
# Or set GOOGLE_APPLICATION_CREDENTIALS to path of service account JSON file